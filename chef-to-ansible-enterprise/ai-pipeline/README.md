Purpose
- Coordinate multi-agent Chef→Ansible migration workflow

Inputs
- Discovery data, exchanges JSON, analysis outputs

Outputs
- Draft artifacts, normalized AAP content, reports, controller bundles

Rules
- JSON in ai-pipeline/exchanges/ is the message bus
- Static analysis (tree-sitter) outputs live in ai-pipeline/analysis/
